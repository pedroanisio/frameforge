---
disclaimer:
  notice: >-
    No information within this document should be taken for granted. Any
    statement or premise not backed by a real logical definition or a
    verifiable reference may be invalid, erroneous, or a hallucination.
  generated_by: "Claude Fable 5 via Claude Code"
  date: "2026-07-17"
---

# FrameForge v2 — HEAD

A single, internally-consistent cut of **FrameForge v2**: a *proposed,
not-yet-conformantly-implemented* agent-native visual-authoring substrate — for
documents, decks, diagrams, books, and letters today, extending toward UIs,
vector graphics, logos, and design systems.

!!! note "What to trust"
    The Pydantic **models are the source of truth**. The JSON Schema, the
    validator, the codemod, and the generated pages — schema reference, grammar,
    spec, SDK guide/API, examples cookbook, fixture gallery, changelog — are
    generated from or checked against them, so those pages can't silently
    drift. Hand-written pages (this one, the tutorial, error codes, the design
    records) are gated for links and disclaimers, not content. The prose and
    grammar are design targets to verify.

## A minimal document

A complete FrameForge document is `dsl` + `version` + at least one page. This
example is **validated in CI** (`tests/test_doc_examples.py` parses every complete
example in the spec, this page, and the README and checks it against the models):

```yaml
dsl: FrameForge
version: "2.6.0"
title: Minimal document
pages:
  - mode: page
    id: hello
    canvas: { size: [320, 200], units: px }
    layers:
      - id: main
        objects:
          - type: rect
            box: [0, 0, 320, 200]
            fill: "#0d9648"
          - type: text
            box: [24, 84, 272, 40]
            text: "Hello, FrameForge"
            style: { color: "#ffffff", font_size: 24, text_align: center }
```

## This site

| Page | What it is | Source |
|---|---|---|
| [Tutorial](tutorial/index.md) | hand-written lessons 01–04 | `docs/tutorial/` |
| [Specification](spec.md) | the normative reference prose | `spec/frameforge-v2-spec.md` |
| [Schema reference](reference.md) | every model & property | **generated** from `schema/frameforge-v2.schema.json` |
| [Grammar](grammar.md) | the consolidated EBNF (a hand-kept *view*) | `grammar/*.ebnf` |
| [SDK guide + API](sdk.md) | authoring SDK docs | **committed generated snapshots** (`tooling/gen_docs.py --sdk`) |
| [Error codes](error-codes.md) | validator findings + SDK rule ids | hand-written (sync-tested) |
| [Examples cookbook](examples.md) | every runnable SDK client | **generated** by `tooling/gen_examples_index.py` |
| [Fixture gallery](fixtures.md) | every fixture rendered to SVG + validator status | **generated** by `tooling/render_fixtures.py` + `tooling/gen_status.py` |
| Design records | architecture, output space, roadmap, ADRs, brand | hand-written |
| [Changelog](changelog.md) | version history & rationale | `CHANGELOG.md` |

The reference, grammar, spec, fixture-gallery, SDK, and changelog pages are
generated by `tooling/gen_docs.py` (`examples.md` by
`tooling/gen_examples_index.py`); the tutorial, error-codes, and design-record
pages are hand-written. Build with `make docs` (it runs the generators first).

## The guarantee

```
src/frameforge/model.py  ──model_json_schema()──►  schema/frameforge-v2.schema.json
        │                                                │
        │ validate.py / pytest                           │ build_schema.py --check
        ▼                                                ▼
   the fixtures (oracle)                          generated reference + gallery
```

`make check` runs every local gate: schema/grammar/spec sync, the assertion
suite, fixture validation, the golden oracle, the fixture-status table, the
overflow/a11y/lint checks, and the documentation drift, link, and disclaimer
gates (see the `check:` target in the Makefile).
