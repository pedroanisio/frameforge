"""Application layer for the rendering bounded context.

Holds the orchestration use-cases that drive the domain resolvers + a painter
adapter to produce output. Today: the `Renderer` document-to-SVG orchestrator.
"""
