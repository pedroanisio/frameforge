node_modules/

# Legacy build artifact from the old `cp ../framegraph-viewer.jsx App.jsx` step.
# entry.jsx now imports the source directly, so this is no longer produced.
dev/App.jsx

# Generated by `npm run verify`; canonical QA screenshots live in verification/.
dev/shot_*.png
